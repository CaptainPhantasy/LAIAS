# PEBKAC Harness Configuration
version: "1.0"
agent_runtime: claude

defaults:
  evidence_required: true
  deterministic_prompting: true
  secrets_isolation: true
  git_guard: true
  checkpoint_interval: 10
